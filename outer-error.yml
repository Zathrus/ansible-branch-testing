- name: Return error
  hosts: localhost
  become: false
  connection: local
  gather_facts: false

  tasks:
    - name: Fail
      ansible.builtin.debug:
        msg:
          - "Task failed successfully, but can we still access stats?"
          - "{{ ip_stat }}"
